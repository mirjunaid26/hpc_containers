Bootstrap: docker
From: ubuntu:22.04

%post
    apt-get update && apt-get install -y bash

%environment
    export GREETING="Hello from Apptainer!"

%runscript
    echo $GREETING
    echo "This container was built on $(date)"
    exec "$@"

%labels
    Author  Student
    Version v1.0